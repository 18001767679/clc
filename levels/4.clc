game=new Game(document.getElementById("game"));
game.originalCode="// We can use function parameters to avoid redundant code!\nhero.moveRight();\n//------------^Type 10 here.";
game.addGoal("Go to the red X",function(){return game.player.x===50&&game.player.y===60});
game.spawnXY("red-cross",50,100);
game.spawnPlayer(50,0);
game.player.load("Simple Boots");
